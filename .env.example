# Postgres connection string, e.g. postgres://user:pass@localhost:5432/activity
DATABASE_URL=

# Shared secret required in the x-api-key header on every route except /health
API_KEY=

# Optional, defaults to 3000
PORT=3000
